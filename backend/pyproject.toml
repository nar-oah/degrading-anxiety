[project]
name = "degrading-anxiety-backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "api",
    "course",
    "schedule",
    "contracts",
    "exam",
]
