[project]
name = "adjustment"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "celery[redis]>=5.6.3",
    "degrading-anxiety-contracts",
]

[tool.uv.sources]
degrading-anxiety-contracts = { workspace = true }
